import { BgColorTypeDirective } from './bg-color-type.directive';

describe('Directive: ColorType', () => {
  it('should create an instance', () => {
    const directive = new BgColorTypeDirective();
    expect(directive).toBeTruthy();
  });
});
