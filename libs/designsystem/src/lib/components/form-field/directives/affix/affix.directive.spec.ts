import { AffixDirective } from './affix.directive';

describe('AffixDirective', () => {
  it('should create an instance', () => {
    const directive = new AffixDirective();
    expect(directive).toBeTruthy();
  });
});
