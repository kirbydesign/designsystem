export interface Translation {
  $code: string;
  back: string;
  close: string;
  nextMonth: string;
  nextSlide: string;
  previousMonth: string;
  previousSlide: string;
  selectYear: string;
  readMore: string;
}
