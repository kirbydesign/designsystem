import { Canvas, Meta } from '@storybook/blocks';

import * as BannerStories from './banner.component.stories';

<Meta of={BannerStories} />

# Banner
The Banner component is x, y, z. It is intended for use in this and that use-case.

By default, it will render as a card with a header, and a placeholder text. 
<Canvas of={BannerStories.Primary} />

If the title-property is set a header with the title is added to the card within the banner.
<Canvas of={BannerStories.Title} />