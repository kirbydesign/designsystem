{
  "name": "@repo/text-resize-lab",
  "version": "0.0.0",
  "private": true,
  "description": "Native iOS reproduction for modal fields covered by the software keyboard.",
  "scripts": {
    "build": "nx run text-resize-lab:build --excludeTaskDependencies",
    "sync": "npm run build && cap sync",
    "ios": "npm run build && cap sync ios && cap open ios",
    "android": "npm run build && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/keyboard": "^8.0.0",
    "@kirbydesign/designsystem-repro": "npm:@kirbydesign/designsystem@11.11.0",
    "inputmask": "5.0.9"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
