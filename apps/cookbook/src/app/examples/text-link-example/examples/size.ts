import { Component, OnInit } from '@angular/core';

const config = {
  selector: 'cookbook-text-link-example-size',
  template: ``,
};

@Component({
  selector: config.selector,
  templateUrl: config.template,
})
export class TextLinkExampleSizeComponent implements OnInit {
  constructor() {}

  ngOnInit() {}
}
