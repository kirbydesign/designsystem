{
    "fontfamily": "{{fontName}}",
    "path": "{{dest}}",
    "icons": [
        {{#each codepoints}}
        {
            "name": "{{@key}}",
            "svg": "{{@key}}.svg",
            "unicode": "\\{{this}}"
        }{{#if @last}}{{else}},{{/if}}
        {{/each}}
    ]
}
