{
    "fontfamily": "'{{fontName}}';",
    "path": "{{dest}}",
    "icons": [
        {{#each codepoints}}
        {
            "name": "{{@key}}",
            "svg": "{{src}}/{{@key}}.svg",
            "unicode": "\\{{this}}"
        }{{#if @last}}{{else}},{{/if}}
        {{/each}}
    ]
}
