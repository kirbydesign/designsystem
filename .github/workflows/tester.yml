name: CI-test

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: branch name modifications
        run: |
          echo "RELEASE_NAME=${GITHUB_REF##*/}" >> @GITHUB_ENV
      - name: print env
        run: echo branch is ${{ env.RELEASE_NAME }}
      - name: print direct
        run: echo branch is $RELEASE_NAME
      - name: src
        run: echo ${GITHUB_REF##*/}
  
