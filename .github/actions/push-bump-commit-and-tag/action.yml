name: 'Push bump commit and tag'
description: '---'
runs: 
  using: 'composite'
  steps:
    - name: Push version bump(s) and tag to Github
      # run: git push --follow-tags
      run: |
        echo "'git push --follow-tags' step has been disabled. Push commits only instead."
        git push
      shell: bash
